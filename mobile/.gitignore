# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Node
node_modules/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# 環境変数（機密情報）
# .env にも EXPO_TOKEN や開発機のURLが入るため明示的に除外する。
# ルートの .gitignore でも拾えているが、この階層だけを見て判断される
# ことがあるため、ここにも書いておく（.easignore と同じ方針）。
.env
.env.local
.env.production

# macOS
.DS_Store

# VSCode
.vscode/

# Android / iOS ビルド成果物
android/
ios/
build/
