node_modules
.expo

# 環境変数（EXPO_TOKEN 等の資格情報を含む）。
# Dockerfile が COPY . . をするため、除外しないとイメージに焼き込まれる。
# backend/.dockerignore（.dev.vars）と web/.dockerignore（.env）に合わせる。
.env
.env.local
.env.production

# 署名鍵・証明書
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# ビルド成果物
android/
ios/
build/
